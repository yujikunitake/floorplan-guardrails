"""Placeholder test.

Exists only so the `test` check has something to collect before Phase 1:
`pytest` exits with code 5 when it finds no tests, which would fail CI.
Delete this file as soon as the real test suite lands.
"""


def test_placeholder() -> None:
    assert True
